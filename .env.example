# Marker PDF Configuration with AWS Bedrock Support

# General Settings
LOGLEVEL=INFO
OUTPUT_DIR=/path/to/output
DEBUG_DATA_FOLDER=/path/to/debug
TORCH_DEVICE=cuda  # Options: cuda, cpu, mps

# Google Gemini API (optional - for comparison)
GOOGLE_API_KEY=your-google-api-key-here

# AWS Bedrock Configuration
# Option 1: Use explicit AWS credentials
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_SESSION_TOKEN=  # Optional, for temporary credentials

# Option 2: Leave empty to use AWS CLI/SDK default credential chain
# (IAM roles, ~/.aws/credentials, environment variables, etc.)

# AWS Bedrock Region
AWS_REGION=us-east-1  # Options: us-east-1, us-west-2, eu-west-1, etc.

# Bedrock Model Selection
# Claude Models (recommended for document processing)
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0
# BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0
# BEDROCK_MODEL_ID=anthropic.claude-3-opus-20240229-v1:0
# BEDROCK_MODEL_ID=anthropic.claude-instant-v1

# Amazon Titan Models (alternative)
# BEDROCK_MODEL_ID=amazon.titan-text-express-v1
# BEDROCK_MODEL_ID=amazon.titan-text-lite-v1

# Other supported models
# BEDROCK_MODEL_ID=ai21.j2-ultra-v1
# BEDROCK_MODEL_ID=ai21.j2-mid-v1
# BEDROCK_MODEL_ID=cohere.command-text-v14
# BEDROCK_MODEL_ID=meta.llama2-13b-chat-v1
# BEDROCK_MODEL_ID=meta.llama2-70b-chat-v1

# Model Configuration
MAX_BEDROCK_TOKENS=8192  # Maximum tokens for response
TEMPERATURE=0.0  # Temperature for text generation (0.0 = deterministic)

# Usage Notes:
# 1. Ensure your AWS account has access to the selected Bedrock model
# 2. Some models require explicit access request in AWS Console
# 3. Pricing varies by model - check AWS Bedrock pricing
# 4. For production, use IAM roles instead of explicit credentials